#ifndef MAIN_H
#define MAIN_H
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <sys/stat.h>
#include <sys/types.h>
#endif

//Adds Data specifically for Key and Value system
int addKV(char *name){

  return EXIT_SUCCESS;
}

//Adds Data specifically for Document Oriented Systems
int addDO(char *name){

  return EXIT_SUCCESS;
}

//Adds data to database
int add(char *name){

  return EXIT_SUCCESS;
}
